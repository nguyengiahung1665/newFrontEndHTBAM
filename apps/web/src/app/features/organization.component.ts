import{ChangeDetectionStrategy,Component,OnInit,inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ApiService}from'../core/api.service';import{AuthService}from'../core/auth.service';import{ModalComponent,PageTitleComponent,RowActionMenuComponent,errorText}from'../shared/ui';
@Component({standalone:true,imports:[FormsModule,ModalComponent,PageTitleComponent,RowActionMenuComponent],changeDetection:ChangeDetectionStrategy.OnPush,template:`
<app-page-title title="Cơ cấu & phân quyền" subtitle="Chức vụ nghiệp vụ của giảng viên trong phạm vi được giao." />@if(error()){<div class="error-box">{{error()}}</div>}@if(message()){<div class="success-box">{{message()}}</div>}
<section class="card table-card"><div class="table-wrap"><table><thead><tr><th>Giảng viên</th><th>Bộ môn</th><th>Chức vụ</th><th>Phạm vi</th><th>Trạng thái</th><th>Thao tác</th></tr></thead><tbody>@for(row of rows();track row.id){<tr><td class="cell-title">{{row.teacherCode}}<small>{{row.fullName}}</small></td><td>{{row.department||'—'}}</td><td>@for(a of row.assignments;track a.id){<span class="badge purple">{{position(a.positionType)}}</span>}@empty{Giảng viên}</td><td>{{row.department||('Khoa #'+row.facultyId)}}</td><td><span class="badge">Hoạt động</span></td><td class="menu-cell"><app-row-action-menu><button type="button" (click)="showInfo(row)">Xem thông tin</button>@if(auth.hasPermission('DEPARTMENT_HEAD_ASSIGN')&&row.departmentId){<button type="button" (click)="begin('department',row)">Gán Trưởng bộ môn</button>}@if(auth.hasPermission('FACULTY_HEAD_TRANSFER')&&row.facultyId){<button type="button" (click)="begin('faculty',row)">Chuyển quyền Trưởng khoa</button>}@for(a of row.assignments;track a.id){@if((a.positionType==='DEPARTMENT_HEAD'&&auth.hasPermission('DEPARTMENT_HEAD_ASSIGN'))||(a.positionType==='FACULTY_HEAD'&&auth.hasPermission('FACULTY_HEAD_TRANSFER'))){<button type="button" class="danger" (click)="beginRevoke(row,a)">Thu hồi {{position(a.positionType)}}</button>}}</app-row-action-menu></td></tr>}@empty{<tr><td colspan="6" class="empty">Không có giảng viên trong phạm vi quản lý.</td></tr>}</tbody></table></div></section>
@if(action()){<app-modal [title]="actionTitle()" [busy]="saving()" size="md" (close)="closeAction()"><p>{{action()!.row.teacherCode}} - {{action()!.row.fullName}}</p>@if(actionError()){<div class="error-box">{{actionError()}}</div>}@if(action()!.type==='info'){<div class="kv"><span>Email</span><strong>{{action()!.row.email||'—'}}</strong><span>Bộ môn</span><strong>{{action()!.row.department||'—'}}</strong></div>}@else{<label>Ghi chú<textarea [(ngModel)]="note" rows="3" placeholder="Lý do hoặc ghi chú thay đổi"></textarea></label>}<div class="modal-footer" style="margin:20px -20px -20px"><button type="button" class="secondary" [disabled]="saving()" (click)="closeAction()">{{action()!.type==='info'?'Đóng':'Hủy'}}</button>@if(action()!.type!=='info'){<button type="button" [class.danger]="action()!.type==='revoke'" [disabled]="saving()" (click)="submit()">{{saving()?'Đang lưu…':'Xác nhận'}}</button>}</div></app-modal>}`})
export class OrganizationComponent implements OnInit{private readonly api=inject(ApiService);readonly auth=inject(AuthService);readonly rows=signal<any[]>([]);readonly error=signal('');readonly message=signal('');readonly action=signal<any|null>(null);readonly actionError=signal('');readonly saving=signal(false);note='';
ngOnInit(){this.load()}load(){this.api.managementAssignments().subscribe({next:rows=>this.rows.set(rows),error:e=>this.error.set(errorText(e))})}position(v:string){return v==='FACULTY_HEAD'?'Trưởng khoa':v==='DEPARTMENT_HEAD'?'Trưởng bộ môn':v}showInfo(row:any){this.begin('info',row)}begin(type:string,row:any){this.action.set({type,row});this.note='';this.actionError.set('')}beginRevoke(row:any,assignment:any){this.action.set({type:'revoke',row,assignment});this.note='';this.actionError.set('')}actionTitle(){return({info:'Thông tin giảng viên',department:'Gán Trưởng bộ môn',faculty:'Chuyển quyền Trưởng khoa',revoke:'Thu hồi chức vụ'}as any)[this.action()?.type]||''}closeAction(){if(!this.saving())this.action.set(null)}
submit(){const a=this.action();if(!a)return;this.saving.set(true);const op=a.type==='department'?this.api.assignDepartmentHead({teacherId:a.row.id,departmentId:a.row.departmentId,note:this.note.trim()||undefined}):a.type==='faculty'?this.api.transferFacultyHead({teacherId:a.row.id,facultyId:a.row.facultyId,note:this.note.trim()||undefined}):this.api.revokeManagementAssignment(a.assignment.id);op.subscribe({next:()=>{this.saving.set(false);this.action.set(null);this.message.set('Đã cập nhật chức vụ.');this.load()},error:e=>{this.saving.set(false);this.actionError.set(errorText(e))}})}}
