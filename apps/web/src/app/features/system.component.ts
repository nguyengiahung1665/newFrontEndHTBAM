import { ChangeDetectionStrategy,Component,OnInit,inject,signal } from '@angular/core';import{forkJoin}from'rxjs';import{ApiService}from'../core/api.service';import{Capabilities}from'../core/models';import{PageTitleComponent,errorText}from'../shared/ui';
@Component({standalone:true,imports:[PageTitleComponent],changeDetection:ChangeDetectionStrategy.OnPush,template:`<app-page-title title="Trạng thái hệ thống" subtitle="Theo dõi sức khỏe các thành phần của HTBAM."><button type="button" class="secondary" (click)="load()">Cập nhật</button></app-page-title>@if(error()){<div class="error-box">{{error()}}</div>}<div class="service-grid">@for(service of services();track service.name){<section class="card" style="margin:0"><div class="service-card" style="padding:0;border:0"><span class="status-dot" [style.background]="service.ok?'#16a34a':'#dc2626'"></span><div><strong>{{service.name}}</strong><small>{{service.description}}</small></div></div><div style="margin-top:14px"><span class="badge" [class.danger]="!service.ok">{{service.ok?'Hoạt động':'Ngoại tuyến'}}</span></div></section>}@empty{<section class="card"><div class="empty-state"><div><strong>Đang kiểm tra dịch vụ</strong><span>Vui lòng chờ phản hồi từ hệ thống.</span></div></div></section>}</div><section class="card" style="margin-top:18px"><div class="card-header" style="margin:-20px -20px 18px"><div><h3>Năng lực AI</h3><p>Thông tin do API capabilities cung cấp</p></div></div><div class="kv"><span>Session inference</span><b>{{caps()?.sessionInference?'Sẵn sàng':'Chưa sẵn sàng'}}</b><span>Face enrollment</span><b>{{caps()?.faceEnrollment?'Sẵn sàng':'Chưa sẵn sàng'}}</b><span>LLM Provider</span><b>{{caps()?.llmProvider||'Chưa cấu hình'}}</b><span>Loaded models</span><b>{{caps()?.loadedModels?.join(', ')||'—'}}</b></div></section>`})
export class SystemComponent implements OnInit{private readonly api=inject(ApiService);readonly caps=signal<Capabilities|null>(null);readonly counts=signal<any>(null);readonly error=signal('');ngOnInit(){this.load()}load(){this.error.set('');forkJoin({caps:this.api.capabilities(),counts:this.api.counts()}).subscribe({next:r=>{this.caps.set(r.caps);this.counts.set(r.counts)},error:e=>this.error.set(errorText(e))})}services(){const c=this.caps();if(!c)return[];return[{name:'Backend',description:'ASP.NET Core API',ok:c.backend},{name:'Database',description:'SQL Server',ok:c.database},{name:'MinIO',description:'Object Storage',ok:c.objectStorage},{name:'AI Service',description:'FastAPI và mô hình AI',ok:c.aiHealth},{name:'Face Recognition',description:'Đăng ký và nhận diện khuôn mặt',ok:c.faceEnrollment},{name:'LLM Provider',description:c.llmProvider||'Chưa cấu hình',ok:c.llmProvider!=='NOT_CONFIGURED'}]}}
